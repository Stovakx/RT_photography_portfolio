<section>
    <div class="container text-center my-4">
        <div class="row my-3">
            <div class="col">
                <h1>
                    Ostatní
                </h1>
            </div>
        </div>
        <div class="row">
            <div class="col">
                <p>
                    Příroda, budovy a vše, co naše země nabídne.
                </p>
            </div>
        </div>
    </div>
</section>



<section class="section_gallery my-5">
    <% if (gallery && gallery.photos && gallery.photos.length > 0) { %>
      <div class="container gallery">
          <%- include('../partials/gallery.ejs') %>
      </div>
    <% } %>
  </section>