<div class="row mt-2">
  <input type="hidden" name="_method" value="PUT">
  <select name="galleryId" class="form-select form-select-sm m-3">
    <% galleries.forEach((gallery) => { %>
      <option value="<%= gallery._id.toString() %>" id="gallerySelect"><%= gallery.name %></option>
    <% }); %>
  </select>
</div>
<!-- doesn't work as should right now -->
<% if (photos && photos.length > 0) { %>
  <div id="photoSortableContainer" <% if (photos.length === 0) { %>style="display: none;"<% } %>>
    <% photos.forEach((photo, index) => { %>
      <% const matchingGalleryPhoto = galleryPhotos.find(galleryPhoto => galleryPhoto.photo.toString() === photo._id.toString() && galleryPhoto.gallery.toString() === galleryId); %>
      <% if (matchingGalleryPhoto) { %>
        <div class="galleryDiv">
          <div class="input-group">
            <img src="/<%= photoBasePath %>/<%= photo.filename %>" alt="<%= photo.name %>" class="imgUpdateForm" />
          </div>
        </div>
      <% } %>
    <% }); %>
  </div>
<% } %>

  <input type="hidden" name="_method" value="PUT">
  